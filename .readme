this project uses postgress db and prisma

please first create a user with a post request to /users with the body structure of: { name : "Some name"}
then take the created id (an autogenerated id), and create a new order in with the url: / with the body structure of:
{
email: string;
fullName: string;
address: string;
imagesUrl: string;
frameColor: string;
userId: number; (your user id)
}
